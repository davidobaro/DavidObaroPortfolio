; Allow postinstall scripts to run in environments where file execute bits
; may be restricted (e.g. Vercel build containers). This avoids permission
; denied errors for native postinstall helpers like napi-postinstall.
unsafe-perm=true

; Disable npm fund messages during CI builds
fund=false
